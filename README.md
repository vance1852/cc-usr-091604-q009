# 球队出行服务

这是职业足球队客场出行编排服务的 Python 起点。`app/travel.py` 目前只保存行程段的基础信息，交通、住宿、审批和费用链路需要后续完善。

## 运行

```bash
python -m unittest discover -s tests -v
```

## 目录

- `app/travel.py`：行程基础对象
- `tests/`：基础行为测试

